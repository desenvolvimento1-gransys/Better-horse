# Railway Nixpacks configuration
# Node version is auto-detected from package.json "engines" field (16.x)

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
